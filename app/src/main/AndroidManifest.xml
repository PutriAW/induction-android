<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.replikalivin" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ReplikaLivin" >
        <activity
            android:name=".ui.HomebaseActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".temp.NewsRecycleViewActivity"
            android:exported="false" />
        <activity
            android:name=".temp.UserDetail"
            android:exported="false" />
        <activity
            android:name=".temp.RecycleViewActivity"
            android:exported="false" />
        <activity
            android:name=".temp.NextActivity"
            android:exported="false" />
        <activity android:name=".temp.BIFastActivity" >

        </activity>
    </application>

</manifest>